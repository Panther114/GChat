{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "offline",
  "description": "Bundled connection-recovery page access to the bounded application commands.",
  "windows": ["main"],
  "permissions": []
}

